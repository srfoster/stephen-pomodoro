#lang racket

(provide (all-defined-out))

(require pomodoro)

(define meta (tag "meta"))
(define break (tag "break"))
(define work (tag "work"))
(define social (tag "social"))
(define writing (tag "writing"))
(define editing (tag "editing"))
(define ltm (tag "LearnToMod"))
(define email (tag "Check Email"))
(define todays-list (tag "Make today's list"))
(define via-email (tag "Task came from someone emailing me"))
(define big-picture (tag "Big picture"))
(define breakdown (tag "Breakdown"))
(define meeting (tag "Meeting"))
(define extension (tag "Extension"))

(define music (tag "Music"))
(define milestone (tag "Milestone"))


